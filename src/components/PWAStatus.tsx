/**
 * PWAStatus component - DISABLED
 * 
 * The sync status is now handled by the unified badge in BottomNav.tsx
 * This component is kept for reference but returns null to prevent
 * duplicate badges and animation conflicts.
 */
export function PWAStatus() {
  // DISABLED: Badge is now integrated into BottomNav
  return null;
}
